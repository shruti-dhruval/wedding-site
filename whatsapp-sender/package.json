{
  "name": "wedding-whatsapp-sender",
  "version": "1.0.0",
  "private": true,
  "description": "Sends queued RSVP invite messages over WhatsApp Web for Shruti & Dhruval's wedding site.",
  "main": "send.js",
  "scripts": {
    "start": "node send.js",
    "dry-run": "node send.js --dry-run",
    "logout": "node logout.js",
    "postinstall": "patch-package"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.0"
  },
  "devDependencies": {
    "patch-package": "^8.0.1"
  }
}
